
Atoms are outside of unit cell. Check:



```Python
import os  
from ase.io.extxyz import read_xyz  
from [ase.io](http://ase.io/) import write  
import tqdm  # Optional: for progress bardef wrap_xyz_file(input_path, output_path):  
    # Read only the first frame from the extended XYZ file  
    atoms = next(read_xyz(open(input_path)))  
    atoms.set_pbc([True, True, True])  
    atoms.wrap()  
    write(output_path, atoms)def wrap_all_xyz_files(input_dir, output_dir):  
    os.makedirs(output_dir, exist_ok=True)    xyz_files = sorted([f for f in os.listdir(input_dir) if f.endswith('.xyz')])    print(f"Wrapping {len(xyz_files)} .xyz files from '{input_dir}' to '{output_dir}'...")    for filename in tqdm.tqdm(xyz_files):  
        input_path = os.path.join(input_dir, filename)  
        output_path = os.path.join(output_dir, filename)  
        try:  
            wrap_xyz_file(input_path, output_path)  
        except Exception as e:  
            print(f"Error processing {filename}: {e}")# Example usage  
input_folder = "/Users/musadu2/Desktop/Sem_1/here_do_it/scott_hydrogen_divided"         # Replace with your input folder name  
output_folder = "/Users/musadu2/Desktop/test/wrapped_xyz"    # Replace with your desired output folderwrap_all_xyz_files(input_folder, output_folder)
```




